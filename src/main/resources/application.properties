spring.application.name=webapp

# Database configuration - can be overridden by environment variables
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3307/eventhub?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Australia/Melbourne}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${DB_USERNAME:root}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${DB_PASSWORD:root123}}
spring.datasource.driver-class-name=${DB_DRIVER_CLASS:com.mysql.cj.jdbc.Driver}

# Flyway settings 
spring.flyway.enabled=true
spring.flyway.url=${SPRING_FLYWAY_URL:jdbc:mysql://localhost:3307/eventhub?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Australia/Melbourne}
spring.flyway.user=${SPRING_FLYWAY_USER:${DB_USERNAME:root}}
spring.flyway.password=${SPRING_FLYWAY_PASSWORD:${DB_PASSWORD:root123}}
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.clean-disabled=false

# Session tracking for logins
server.servlet.session.tracking-modes=cookie

# File upload configuration
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=25MB
spring.servlet.multipart.enabled=true

# Photo upload directory - store images in mysql_data folder
app.upload.dir=mysql_data/event_photos

google.maps.api.key=AIzaSyC9AOLHidqA4OW4GOXCUWm0icbq5FFLiLY

app.email.enabled=true
app.email.from=noreply@eventhub.rmit.edu.au

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sept2025.group.05@gmail.com
spring.mail.password=tnflxbaqtywwcnrs
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com